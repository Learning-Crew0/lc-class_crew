/**
 * Course Display Tag Constants
 * Used for filtering courses in the frontend
 */

const COURSE_DISPLAY_TAGS = {
    NEWEST: "NEWEST",
    POPULAR: "POPULAR",
    ALL: "ALL",
};

const COURSE_DISPLAY_TAG_VALUES = Object.values(COURSE_DISPLAY_TAGS);

module.exports = {
    COURSE_DISPLAY_TAGS,
    COURSE_DISPLAY_TAG_VALUES,
};

